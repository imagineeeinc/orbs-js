/*
    if (this.type == mesh) {
      if (this.drawType == rect) {
        this.x = opts[0].x
        this.y = opts[0].y
        this.width = opts[0].width
        this.height = opts[0].height
        this.color = opts[0].color
        this.scale = opts[0].scale || 1
      } else if (this.drawType == circle) {
        this.x = opts[0].x
        this.y = opts[0].y
        this.width = opts[0].width
        this.color = opts[0].color
        this.scale = opts[0].scale || 1
      }
    } else if (this.type == sprite) {
      if (this.drawType == texture) {
        this.x = opts[0].x
        this.y = opts[0].y
        this.width = opts[0].width
        this.height = opts[0].height
        this.sprite = opts[0].sprite
        this.scale = opts[0].scale || 1
        this.dx = opts[0].dx || 0
      }
    } else if (this.type == text) {
      if (this.drawType == plainText) {
        this.x = opts[0].x
        this.y = opts[0].y
        this.txt = opts[0].txt
        this.font = opts[0].font
        this.color = opts[0].color || "black"
        this.scale = opts[0].scale || 1
      }
    } else if (this.type == line) {
      this.start = opts[0].start
      this.end = opts[0].end
      this.width = opts[0].width
      this.color = opts[0].color || "black"
    } else if (this.type == customMesh) {
      this.meshShader = opts[0]
    }*/